<%= render 'layouts/header' %>
<%= render @project %>
 <div class="blog-card">
    <div class="meta">
      <div class="photo" style="background-image: url(https://storage.googleapis.com/chydlx/codepen/blog-cards/image-1.jpg)"></div>
      <ul class="details">
        <li class="Manager"><a href="#"><%= @project.creator.full_name %></a></li>
        <li class="date"><%= @project.created_at.to_date %></li>
        <li class="enrolled user"><%= @project.enrolled_user.count %></li>
      <ul>
           

<div>
</div>
          </ul>
        </li>
      </ul>
    </div>
    <div class="description">
      <h1><%= @project.title %></h1>
      <p> <%= @project.description %></p>
      <p class="read-more">
         <% if policy(@project).delete? %>
                <%= button_to @project, method: :delete, data: {confirm: "Are you sure you want to delete this user?"}, class: "btn btn-responsive btn-default btn-sm mdb-color darken-3 " do %>Delete
                <% end %>
              <% end %>
              
              <% if policy(@project).edit? %>
                <%= link_to edit_project_path(@project), class: "btn btn-responsive btn-default btn-sm mdb-color darken-3" do %>Edit
                <% end %>
              <% end %>
              
        <%= link_to "Back to projects", projects_path %></a>
      </p>
    </div>
  </div>
  <div class="blog-card alt">
    <div class="meta">
      <div class="photo" style="background-image: url(https://storage.googleapis.com/chydlx/codepen/blog-cards/image-2.jpg)"></div>
    </div>
  </div>


